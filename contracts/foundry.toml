[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
hedera_testnet = "${HEDERA_RPC_URL}"

[etherscan]
hedera_testnet = { key = "", url = "https://server-testnet.hashscan.io/api" }
