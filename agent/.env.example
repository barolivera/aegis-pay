# NETWORK=local (default) or NETWORK=hedera
NETWORK=local

# For Hedera testnet (ignored when NETWORK=local)
AGENT_PRIVATE_KEY=0x_your_ecdsa_key
TARGET_ADDRESS=0x_target_evm_address

# Contract addresses (set after deploying to Hedera)
# For local, addresses.json is used automatically
